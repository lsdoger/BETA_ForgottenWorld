<Window x:Class="Launcher.LoggedInPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="LoggedInPage"
        Height="450"
        Width="800"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <Style TargetType="ListBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="ItemBorder" 
                                Background="{TemplateBinding Background}"
                                BorderBrush="White"
                                BorderThickness="1"
                                CornerRadius="4"
                                Padding="12,8">
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="#33FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="White"/>
                                <Setter Property="Foreground" Value="Black"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ListBox">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Background="#CC000000"
                BorderBrush="#33FFFFFF"
                BorderThickness="0,0,0,1"
                MouseDown="TopMenu_MouseDown">
            <Grid>
                <TextBlock Text="ForgottenWorld"
                           Foreground="White"
                           VerticalAlignment="Center"
                           Margin="15,0,0,0"
                           FontWeight="Bold"/>

                <Button Content="✕"
                        HorizontalAlignment="Right"
                        Width="40"
                        Background="Transparent"
                        Foreground="White"
                        BorderThickness="0"
                        Click="Close_Click"
                        Cursor="Hand"/>
            </Grid>
        </Border>

        <Grid Grid.Row="1">

            <Image Source="pack://application:,,,/Assets/bg.jpg"
                   Stretch="UniformToFill"/>

            <Rectangle>
                <Rectangle.Fill>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#CC000000" Offset="0"/>
                        <GradientStop Color="#99000000" Offset="1"/>
                    </LinearGradientBrush>
                </Rectangle.Fill>
            </Rectangle>

            <Grid Margin="60,0,60,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="300"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" 
                            VerticalAlignment="Center" 
                            HorizontalAlignment="Left">

                    <TextBlock Text="WELCOME BACK,"
                               Foreground="#AAAAAA"
                               FontSize="14"
                               FontWeight="SemiBold"/>

                    <TextBlock x:Name="UsernameText"
                               Text="PLAYER"
                               Foreground="White"
                               FontSize="48"
                               FontWeight="Bold"
                               Margin="0,-5,0,30">
                        <TextBlock.Effect>
                            <DropShadowEffect Color="Black" BlurRadius="15" ShadowDepth="2" Opacity="0.8"/>
                        </TextBlock.Effect>
                    </TextBlock>

                    <Button Content="PLAY SELECTED"
                            Width="250"
                            Height="55"
                            HorizontalAlignment="Left"
                            FontWeight="Bold"
                            FontSize="16"
                            Foreground="White"
                            Background="#22FFFFFF"
                            BorderBrush="White"
                            BorderThickness="1"
                            Cursor="Hand"
                            Click="Play_Click">

                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border x:Name="border" 
                                                    Background="{TemplateBinding Background}" 
                                                    BorderBrush="{TemplateBinding BorderBrush}" 
                                                    BorderThickness="{TemplateBinding BorderThickness}" 
                                                    CornerRadius="6">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="border" Property="Background" Value="#44FFFFFF"/>
                                                </Trigger>
                                                <Trigger Property="IsPressed" Value="True">
                                                    <Setter TargetName="border" Property="Background" Value="White"/>
                                                    <Setter Property="Foreground" Value="Black"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>

                <Grid Grid.Column="1" VerticalAlignment="Center">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="SELECT SERVER"
                               Foreground="#AAAAAA"
                               FontSize="12"
                               FontWeight="Bold"
                               Margin="0,0,0,15"/>

                    <ListBox Grid.Row="1" 
                             x:Name="ServersList"
                             Background="Transparent"
                             BorderThickness="0"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled">

                        <ListBoxItem Content="Vanilla Server"/>
                        <ListBoxItem Content="Tech Forge" IsSelected="True"/>
                        <ListBoxItem Content="Magic RPG"/>
                        <ListBoxItem Content="Hardcore Mode"/>

                    </ListBox>
                </Grid>

            </Grid>
        </Grid>
    </Grid>
</Window>